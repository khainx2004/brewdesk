# Biến môi trường — chứa mật khẩu DB và JWT secret, KHÔNG commit
.env
*.env
!.env.example

# Backend — Maven
source/backend/target/
source/backend/.mvn/wrapper/maven-wrapper.jar
*.class
*.jar
!source/backend/.mvn/wrapper/*.properties

# Frontend — Node
source/frontend/node_modules/
source/frontend/dist/
source/frontend/.vite/
npm-debug.log*
yarn-error.log*

# IDE
.idea/
*.iml
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Claude Code — settings.json dùng chung thì commit, .local là của riêng từng
# máy (chứa đường dẫn cá nhân và có thể lẫn credential) nên giữ ngoài repo
.claude/settings.local.json
